<div class="container">
    <div class="card">
        <div class="card-header">
            <h3>
                <?php if ($user['id']): ?>
                    Update user <b><?php echo $user['firstName'] ?></b>
                <?php else: ?>
                    Create new User
                <?php endif ?>
            </h3>
        </div>
        <div class="card-body">

            <form method="POST" enctype="multipart/form-data"
                  action="">
                <div class="form-group">
                    <label>Name</label>
                    <input name="firstName" value="<?php echo $user['firstName'] ?>"
                           class="form-control <?php echo $errors['firstName'] ? 'is-invalid' : '' ?>">
                    <div class="invalid-feedback">
                        <?php echo  $errors['firstName'] ?>
                    </div>
                </div>
                <div class="form-group">
                    <label>lastName</label>
                    <input name="lastName" value="<?php echo $user['lastName'] ?>"
                           class="form-control <?php echo $errors['lastName'] ? 'is-invalid' : '' ?>">
                    <div class="invalid-feedback">
                        <?php echo  $errors['lastName'] ?>
                    </div>
                </div>
                <div class="form-group">
                    <label>Email</label>
                    <input name="email" value="<?php echo $user['email'] ?>"
                           class="form-control  <?php echo $errors['email'] ? 'is-invalid' : '' ?>">
                    <div class="invalid-feedback">
                        <?php echo  $errors['email'] ?>
                    </div>
                </div>
                <div class="form-group">
                    <label>Password</label>
                    <input name="password" value="<?php echo $user['password'] ?>"
                           class="form-control  <?php echo $errors['password'] ? 'is-invalid' : '' ?>">
                    <div class="invalid-feedback">
                        <?php echo  $errors['password'] ?>
                    </div>
                </div>
                <div class="form-group">
                    <label>clearanceLevel</label>
                    <input name="clearanceLevel" value="<?php echo $user['clearanceLevel'] ?>"
                           class="form-control  <?php echo $errors['clearanceLevel'] ? 'is-invalid' : '' ?>">
                    <div class="invalid-feedback">
                        <?php echo  $errors['clearanceLevel'] ?>
                    </div>
                </div>

                <button class="btn btn-success">Submit</button>
            </form>
        </div>
    </div>
</div>
